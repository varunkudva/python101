# Largest subsequence in a string which is a palindrome
def palindrome_check(string):
    i = 0
    j = len(string)
    while (i < j):
        if string[i] != string[j]
           return False
        i += 1
        j -= 1
    if i >= j: return True


for i in range(0, len(string)):

